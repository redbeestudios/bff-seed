echo "Running pre-commit checks..."

echo "Type-checking with TypeScript..."
npm run typecheck || { echo "TypeScript errors found. Fix them before committing."; exit 1; }

echo "Linting staged files..."
npm run lint-staged || { echo "Linting errors found. Fix them before committing."; exit 1; }

echo "All checks passed! Proceeding with commit..."
